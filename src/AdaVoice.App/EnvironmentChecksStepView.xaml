<UserControl x:Class="AdaVoice.App.EnvironmentChecksStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <StackPanel>
        <TextBlock Text="Environment checks" Style="{StaticResource SectionTitleStyle}" />

        <!-- Brief spinner shown before results appear. The checks actually run instantly — this is
             a deliberate cosmetic pause (design 05 §2: "each row: spinner → ✓/✗").
             Swapped for ChecksList by the timer in EnvironmentChecksStepView.xaml.cs. -->
        <StackPanel x:Name="SpinnerPanel" Orientation="Horizontal" Margin="0,0,0,8">
            <ProgressBar IsIndeterminate="True" Width="16" Height="16" />
            <TextBlock Text="Checking…" Margin="8,0,0,0" VerticalAlignment="Center"
                       Foreground="{DynamicResource Text.Secondary}" />
        </StackPanel>

        <ItemsControl x:Name="ChecksList" Visibility="Collapsed" ItemsSource="{Binding Checks}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource PanelStyle}" Margin="0,0,0,8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                <TextBlock Margin="8,0,0,0" Text="{Binding Status, Converter={StaticResource CheckStatusToLabel}}"
                                           Foreground="{Binding Status, Converter={StaticResource CheckStatusToBrush}}" />
                            </StackPanel>
                            <TextBlock Text="{Binding Detail}" Foreground="{DynamicResource Text.Secondary}"
                                       Margin="0,4,0,0" TextWrapping="Wrap" />
                            <TextBlock Margin="0,4,0,0"
                                       Visibility="{Binding Converter={StaticResource FailedCableCheckToLink}}">
                                <Hyperlink NavigateUri="https://vb-audio.com/Cable/" RequestNavigate="OnVbCableLinkRequestNavigate">
                                    Download VB-CABLE
                                </Hyperlink>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ui:Button Content="Re-check" Appearance="Secondary" Command="{Binding RecheckCommand}"
                   HorizontalAlignment="Left" />
    </StackPanel>
</UserControl>
