<ui:FluentWindow x:Class="AdaVoice.App.RepairPhraseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Repair phrase"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="None"
        WindowCornerPreference="Round"
        Background="{DynamicResource Surface.Window}"
        TextElement.Foreground="{DynamicResource Text.Primary}"
        FontFamily="Segoe UI Variable, Segoe UI" FontSize="{StaticResource FontSize.Body}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="Repair phrase" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" />

        <StackPanel Grid.Row="1" Margin="{StaticResource Space.DialogContent}">
        <TextBlock Text="{Binding Title}" FontWeight="SemiBold"
                   FontSize="{StaticResource FontSize.SectionTitle}" TextWrapping="Wrap" />
        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="⚠" Foreground="{DynamicResource Status.OffAir}" Margin="0,0,6,0" />
            <TextBlock Text="This phrase's audio file is missing." Foreground="{DynamicResource Text.Secondary}"
                       TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="{StaticResource Space.ButtonRow}">
            <ui:Button Content="Cancel" Appearance="Secondary" IsCancel="True" Margin="0,0,8,0" />
            <ui:Button Content="Remove" Appearance="Secondary" Margin="0,0,8,0" Click="Remove_Click" />
            <ui:Button Content="Re-record" Appearance="Primary" IsDefault="True" Click="ReRecord_Click" />
        </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
