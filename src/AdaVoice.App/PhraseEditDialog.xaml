<Window x:Class="AdaVoice.App.PhraseEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Edit phrase"
        Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{StaticResource Surface.Window}"
        TextElement.Foreground="{StaticResource Text.Primary}"
        FontFamily="Segoe UI Variable, Segoe UI" FontSize="14">
    <StackPanel Margin="16">
        <TextBlock Text="Title" Foreground="{StaticResource Text.Secondary}" Margin="0,0,0,4" />
        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Category" Foreground="{StaticResource Text.Secondary}" Margin="0,12,0,4" />
        <ComboBox ItemsSource="{Binding Categories}"
                  SelectedValue="{Binding SelectedCategoryId}"
                  SelectedValuePath="Id">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Border Width="16" Height="16" CornerRadius="3" VerticalAlignment="Center"
                                Background="{Binding Color, Converter={StaticResource HexToBrush}}" />
                        <TextBlock Text="{Binding Name}" Margin="8,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="Tags" Foreground="{StaticResource Text.Secondary}" Margin="0,12,0,4" />

        <!-- Current tags as removable chips -->
        <ItemsControl ItemsSource="{Binding Tags}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource Surface.Raised}" CornerRadius="10"
                            Padding="8,3" Margin="0,0,6,6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                            <Button Content="✕" Margin="6,0,0,0" Padding="0" BorderThickness="0"
                                    Background="Transparent" Foreground="{StaticResource Text.Secondary}"
                                    Cursor="Hand" ToolTip="Remove tag"
                                    Command="{Binding DataContext.RemoveTagCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Add a new tag -->
        <Grid Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox Grid.Column="0" PlaceholderText="Add a tag…"
                        Text="{Binding NewTag, UpdateSourceTrigger=PropertyChanged}">
                <ui:TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding AddTagCommand}" />
                </ui:TextBox.InputBindings>
            </ui:TextBox>
            <ui:Button Grid.Column="1" Content="Add" Appearance="Secondary" Margin="8,0,0,0"
                       Command="{Binding AddTagCommand}" />
        </Grid>

        <!-- Reuse an existing tag -->
        <ItemsControl ItemsSource="{Binding Suggestions}" Margin="0,8,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Margin="0,0,6,6" Padding="8,3" Cursor="Hand"
                            BorderThickness="1" BorderBrush="{StaticResource Text.Secondary}"
                            Background="Transparent" Foreground="{StaticResource Text.Secondary}"
                            ToolTip="Add this existing tag"
                            Command="{Binding DataContext.AddSuggestionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <ui:Button Content="Cancel" Appearance="Secondary" IsCancel="True" Margin="0,0,8,0" />
            <ui:Button x:Name="SaveButton" Content="Save" Appearance="Primary"
                       IsDefault="True" Click="Save_Click"
                       IsEnabled="{Binding HasValidTitle}" />
        </StackPanel>
    </StackPanel>
</Window>
