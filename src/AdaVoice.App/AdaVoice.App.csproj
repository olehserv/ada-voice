<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- The WPF operator app (Phase 3). Reuses EngineHost from AdaVoice.Host as the composition root. -->
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <ApplicationIcon>Resources\AppIcon.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\AdaVoice.Host\AdaVoice.Host.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Also a WPF Resource (not just the exe's Win32 icon) so Window.Icon can pack-URI it below. -->
    <Resource Include="Resources\AppIcon.ico" />
  </ItemGroup>

  <!--
    Localization (en/uk/pl). Strings.resx (neutral = English) + Strings.uk.resx/Strings.pl.resx
    (satellites, by naming convention — no extra config needed) compile automatically via the
    SDK's default EmbeddedResource glob. The strongly typed accessor is Resources/Strings.cs,
    hand-written rather than VS-generated: PublicResXFileCodeGenerator is a Visual Studio
    "single file generator" feature that `dotnet build` never invokes (confirmed empirically —
    a CLI build compiles the .resources/satellite assemblies fine but produces no Designer.cs).
  -->

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" />
    <PackageReference Include="WPF-UI" />
    <PackageReference Include="Serilog" />
    <PackageReference Include="Serilog.Sinks.File" />
  </ItemGroup>

</Project>
