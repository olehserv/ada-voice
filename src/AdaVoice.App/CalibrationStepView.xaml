<UserControl
    x:Class="AdaVoice.App.CalibrationStepView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <StackPanel>
        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Voice calibration" />
        <TextBlock
            Margin="0,0,0,12"
            Foreground="{DynamicResource Text.Secondary}"
            Text="Speak normally for 5 seconds so AdaVoice can match your recorded phrases to your live voice level."
            TextWrapping="Wrap" />

        <ui:Button
            HorizontalAlignment="Left"
            Command="{Binding StartCalibrationCommand}"
            Content="Start"
            Style="{StaticResource BrandCtaButtonStyle}"
            Visibility="{Binding CanStart, Converter={StaticResource BoolToVis}}" />

        <Grid
            Width="44"
            Height="44"
            Margin="0,0,0,8"
            HorizontalAlignment="Left"
            Visibility="{Binding IsRecording, Converter={StaticResource BoolToVis}}">
            <!--  Static background track.  -->
            <Ellipse
                Width="44"
                Height="44"
                Stroke="{DynamicResource Text.Secondary}"
                StrokeThickness="4" />

            <!--
                Foreground ring: one dash and one gap, each equal to the full circumference (in
                StrokeThickness-relative units: 125.664 / 4 = 31.416), rotated -90 degrees so the
                drain starts at 12 o'clock. StrokeDashOffset animates 0 -> 31.416 (same
                thickness-relative units as StrokeDashArray) over exactly 5 seconds while
                IsRecording is true.
            -->
            <Ellipse
                x:Name="CountdownRing"
                Width="44"
                Height="44"
                RenderTransformOrigin="0.5,0.5"
                Stroke="{DynamicResource Accent}"
                StrokeDashArray="31.416,31.416"
                StrokeDashOffset="0"
                StrokeThickness="4">
                <Ellipse.RenderTransform>
                    <RotateTransform Angle="-90" />
                </Ellipse.RenderTransform>
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard x:Name="CountdownBeginStoryboard">
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="StrokeDashOffset"
                                                From="0"
                                                To="31.416"
                                                Duration="0:0:5" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                                <DataTrigger.ExitActions>
                                    <StopStoryboard BeginStoryboardName="CountdownBeginStoryboard" />
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
        </Grid>

        <TextBlock
            Margin="0,0,0,8"
            FontWeight="SemiBold"
            Text="Recording… speak now"
            Visibility="{Binding IsRecording, Converter={StaticResource BoolToVis}}" />

        <TextBlock
            FontWeight="SemiBold"
            Foreground="{DynamicResource Status.Live}"
            Text="Voice level captured ✓"
            Visibility="{Binding Succeeded, Converter={StaticResource BoolToVis}}" />

        <StackPanel Visibility="{Binding HasMessage, Converter={StaticResource BoolToVis}}">
            <TextBlock
                Margin="0,4,0,8"
                Foreground="{DynamicResource Status.Degraded}"
                Text="{Binding Result.Message}"
                TextWrapping="Wrap" />
            <ui:Button
                HorizontalAlignment="Left"
                Appearance="Secondary"
                Command="{Binding StartCalibrationCommand}"
                Content="Try again" />
        </StackPanel>
    </StackPanel>
</UserControl>
